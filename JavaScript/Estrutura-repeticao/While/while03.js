let contador = 10;


// Enquanto contador for maior que 0 o bloco de código será executado
while (contador > 0) {
    console.log( contador );
    contador--; //Aqui a variável contador está sofrendo decremento o que diminui seu valor em 1 unidade
}