//Quando for necessário separar uma string em substrings, basta usar o metodo split()

//O método split() separa a variável cores_string em substrings menores. A cada espaço em branco que essa string tiver, ele cria um novo elemento

// Com o método split() Quebramos a strinf. com isso podemos acessar o primeiro elemento do array. para obter o nome da string


let cores_string = "Vermelho Azul Preto Verde"

let corres_array = cores_string.split(" "); //(Caractere usado para separar a string)

console.log(corres_array)

// Utilizamos o método split() para separar as strings em partes.
