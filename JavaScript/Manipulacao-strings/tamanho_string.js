let cep = "14535-000"

// if ternario 
let resultado = cep.length != 9  ? ("\no CEP deve ter 9 caracteres\n") : ("\no CEP está correto\n");

console.log(resultado)

// Utizamos o length para obter o tamanho da string 