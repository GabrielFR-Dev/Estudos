import "./globals.css";

export const metadata = {
  title: "Exemplo State Hook onClick 01",
  description: "Exemplo de uso do State Hook com evento onClick em React",
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-br">
      <body>
        {children}
      </body>
    </html>
  );
}
